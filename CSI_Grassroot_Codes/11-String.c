#include <stdio.h>
#include <string.h>

int main() {
    char str[20] = "Grassroots";
    int length;
    length = strlen(str);
    printf("The string is : %s\n", str);
    printf("Length = %d\n", length);
    return 0;
}
